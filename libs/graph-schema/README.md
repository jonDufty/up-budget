# graph-schema

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build graph-schema` to build the library.

## Running unit tests

Run `nx test graph-schema` to execute the unit tests via [Jest](https://jestjs.io).
